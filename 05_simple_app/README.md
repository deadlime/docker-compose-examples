# Simple App

Install client dependencies:

```
$ cd client/
$ npm install
$ cd ..
```

Initialize database:

```
$ docker-compose run --rm server python -m app
```

Start application:

```
$ docker-compose up
```
